{
  "extends": "../../packages/tsconfig.json", // Extends the shared package config
  "compilerOptions": {
    "outDir": "dist",
    "paths": {
      "database": ["../../packages/database"]
    }
  },
  "include": ["src/**/*"]
}